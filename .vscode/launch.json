{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Ionic: Debug in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8100",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
